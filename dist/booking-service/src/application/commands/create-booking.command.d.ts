export declare class CreateBookingCommand {
    readonly userId: string;
    readonly eventId: string;
    readonly bookingId: string;
    constructor(userId: string, eventId: string, bookingId: string);
}
